<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.mz.policemanager">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.PoliceManager">
        <activity android:name=".activity.MultaDetalhesActivity"></activity>
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.SettingActivity" />
        <activity android:name=".activity.ReportActivity" />
        <activity android:name=".activity.PropertyActivity" />
        <activity android:name=".activity.PrivacityActivity" />
        <activity android:name=".activity.PoliceActivity" />
        <activity android:name=".activity.NewPoliceActivity" />
        <activity android:name=".activity.NewDriverActivity" />
        <activity android:name=".activity.NewCarActivity" />
        <activity android:name=".activity.MultaActivity" />
        <activity android:name=".activity.EmitirMultaActivity" />
        <activity android:name=".activity.DriverActivity" />
        <activity android:name=".activity.CarActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>